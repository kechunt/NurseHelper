/**
 * Matrices de `imports: [...]` usadas en NurseHelper.
 * Copia solo el bloque que corresponda a tu pantalla.
 */

// --- Solo listados / pipes (ngIf, ngFor, date, async) ---
// imports: [CommonModule]

// --- Formulario template-driven (ngModel) ---
// imports: [CommonModule, FormsModule]

// --- Formulario reactivo + listados + búsqueda con debounce + paginación ---
// import { ReactiveFormsModule } from '@angular/forms';
// import { PaginationComponent } from '../../shared/components/pagination/pagination.component';
// import { DebounceDirective } from '../../shared/directives/debounce.directive';
// imports: [CommonModule, FormsModule, ReactiveFormsModule, PaginationComponent, DebounceDirective]

// --- Login / registro / verify (navegación + formulario) ---
// import { RouterModule } from '@angular/router';
// imports: [CommonModule, FormsModule, RouterModule]

// --- Shell raíz (router + toasts + loading + confirmación) ---
// import { RouterOutlet } from '@angular/router';
// import { ToastContainerComponent } from './components/toast-container/toast-container.component';
// import { LoadingSpinnerComponent } from './components/loading-spinner/loading-spinner.component';
// import { ConfirmationWrapperComponent } from './components/confirmation-wrapper/confirmation-wrapper.component';
// imports: [RouterOutlet, CommonModule, ToastContainerComponent, LoadingSpinnerComponent, ConfirmationWrapperComponent]
