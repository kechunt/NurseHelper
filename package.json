{
  "name": "nurse-helper",
  "version": "1.0.0",
  "description": "Sistema de gestión de enfermería - Frontend y Backend",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c cyan,yellow \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test:frontend": "cd frontend && npm test",
    "migration:run": "cd backend && npm run migration:run",
    "migration:generate": "cd backend && npm run migration:generate"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}