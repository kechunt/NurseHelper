export const environment = {
  production: true,
  // ⚠️ IMPORTANTE: Actualiza esta URL con la URL pública de tu backend en Railway
  // La URL debe ser pública (https://tu-backend.railway.app), NO interna (railway.internal)
  // Para obtenerla: Railway → Tu servicio backend → Settings → Networking → Generate Domain
  // Ejemplo: 'https://nursehelper-production.up.railway.app/api'
  apiUrl: 'https://nursehelper-production.up.railway.app/api'
};

